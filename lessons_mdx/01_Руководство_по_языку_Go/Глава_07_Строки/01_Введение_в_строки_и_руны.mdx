# Строки и руны в Go

<Meta>
reading_time: 9
</Meta>

<Overview>
1. **Строка (string)** — неизменяемая последовательность байтов в кодировке UTF-8
2. **Руна (rune)** — Unicode код-поинт, псевдоним для `int32`
3. **len(s)** — возвращает количество БАЙТ, не символов!
4. **Неизменяемость** — строку нельзя изменить напрямую, только создать новую
5. **UTF-8** — один символ может занимать от 1 до 4 байт
</Overview>

<Theory>
### Строки в Go — не то, что кажется

В большинстве языков строка — это массив символов. В Go **строка — это неизменяемая последовательность байтов**. Это фундаментальное отличие.

```go
s := "Hello"
// s[0] = 'h'  // ОШИБКА! Строки неизменяемы
```

### Что такое UTF-8?

**UTF-8** — способ кодирования Unicode-символов байтами:
- ASCII-символы (a-z, 0-9): 1 байт
- Кириллица: 2 байта
- Китайские иероглифы: 3 байта
- Emoji: 4 байта

```go
fmt.Println(len("Hello")) // 5 (5 ASCII-символов = 5 байт)
fmt.Println(len("Привет")) // 12 (6 кириллических = 12 байт)
fmt.Println(len("😀"))     // 4 (1 emoji = 4 байта)
```

### Руна (rune) — один символ

`rune` — это псевдоним для `int32`, хранит один Unicode код-поинт:

```go
var r rune = 'П'         // код 1055
fmt.Printf("%c %d\n", r, r) // П 1055
```

### Главная ловушка: len() возвращает байты!

```go
s := "Привет"
fmt.Println(len(s))         // 12 (байт!)
fmt.Println(len([]rune(s))) // 6 (символов)
```

### Итерация по строке

**По байтам** (редко нужно):
```go
for i := 0; i < len(s); i++ {
    fmt.Printf("%x ", s[i]) // d0 9f d1 80 ...
}
```

**По рунам** (обычно это нужно):
```go
for i, r := range s {
    fmt.Printf("%d: %c\n", i, r)
}
// 0: П
// 2: р  // индекс 2, не 1!
// 4: и
// ...
```

### Преобразования

```go
s := "Hello"
bytes := []byte(s)    // строка → байты
runes := []rune(s)    // строка → руны
s2 := string(bytes)   // байты → строка
s3 := string(runes)   // руны → строка
```

### Как изменить строку?

Через преобразование в срез:
```go
s := "Hello"
runes := []rune(s)
runes[0] = 'h'
s = string(runes)  // "hello"
```

Или с помощью `strings.Builder`:
```go
var b strings.Builder
b.WriteString("Hello")
b.WriteString(" World")
s := b.String()
```

### Сырые строки (raw strings)

Обратные кавычки сохраняют всё как есть:
```go
s := `Строка с "кавычками"
и переносами строк
\n не экранируется`
```

Полезно для JSON, SQL, регулярных выражений.
</Theory>

<Syntax>
### Объявление строк

```go
s1 := "Hello"              // строка в двойных кавычках
s2 := `Raw string
with newlines`             // "сырая" строка в обратных кавычках
var s3 string              // пустая строка ""
```

### Объявление рун

```go
r1 := 'A'                  // руна (символ)
r2 := '\n'                 // управляющий символ
r3 := '\u0041'             // Unicode: A
r4 := '\U0001F600'         // Unicode emoji: 😀
var r5 rune = 1055         // код символа 'П'
```

### Преобразования

```go
[]byte(s)                  // строка → срез байт
[]rune(s)                  // строка → срез рун
string(bytes)              // срез байт → строка
string(runes)              // срез рун → строка
string(r)                  // руна → строка
```
</Syntax>

<Examples>
кода

### Базовая работа со строками

```go
package main

import "fmt"

func main() {
    // Разные строки
    eng := "Hello World"
    rus := "Привет мир"
    chi := "你好世界"
    emoji := "Hello 😀"
    
    // len() возвращает БАЙТЫ, не символы!
    fmt.Println("English:", len(eng))  // 11 (1 байт на символ)
    fmt.Println("Russian:", len(rus))  // 19 (2 байта на кириллицу)
    fmt.Println("Chinese:", len(chi))  // 12 (3 байта на иероглиф)
    fmt.Println("Emoji:", len(emoji))  // 10 (4 байта на emoji)
}
```

### Длина в символах vs байтах

```go
package main

import (
    "fmt"
    "unicode/utf8"
)

func main() {
    s := "Привет, мир! 🌍"
    
    // Длина в байтах
    fmt.Println("Байт:", len(s))  // 27
    
    // Длина в символах (рунах)
    fmt.Println("Символов:", utf8.RuneCountInString(s))  // 14
    
    // Или через преобразование в []rune
    fmt.Println("Рун:", len([]rune(s)))  // 14
}
```

### Перебор строки по символам (рунам)

```go
package main

import "fmt"

func main() {
    s := "Привет"
    
    // Способ 1: for-range (рекомендуется)
    fmt.Println("for-range:")
    for i, r := range s {
        fmt.Printf("  [%d] %c (U+%04X)\n", i, r, r)
    }
    
    // Способ 2: через []rune
    fmt.Println("\n[]rune:")
    runes := []rune(s)
    for i, r := range runes {
        fmt.Printf("  [%d] %c\n", i, r)
    }
}
```

Вывод:
```
for-range:
  [0] П (U+041F)
  [2] р (U+0440)
  [4] и (U+0438)
  [6] в (U+0432)
  [8] е (U+0435)
  [10] т (U+0442)

[]rune:
  [0] П
  [1] р
  [2] и
  [3] в
  [4] е
  [5] т
```

### Перебор по байтам (осторожно!)

```go
package main

import "fmt"

func main() {
    s := "Привет"
    
    // ❌ По байтам — некорректно для UTF-8
    fmt.Println("По байтам:")
    for i := 0; i < len(s); i++ {
        fmt.Printf("%c", s[i])  // Ð Ñ Ð ¸ Ð ² ...
    }
    fmt.Println()
    
    // ✅ По рунам — корректно
    fmt.Println("По рунам:")
    for _, r := range s {
        fmt.Printf("%c", r)  // Привет
    }
    fmt.Println()
}
```

### Работа с рунами

```go
package main

import "fmt"

func main() {
    // Объявление рун
    r1 := 'A'
    r2 := 'П'
    r3 := '😀'
    
    fmt.Printf("'A': символ=%c, код=%d, hex=%X\n", r1, r1, r1)
    fmt.Printf("'П': символ=%c, код=%d, hex=%X\n", r2, r2, r2)
    fmt.Printf("'😀': символ=%c, код=%d, hex=%X\n", r3, r3, r3)
    
    // Руна → строка
    s1 := string(r1)
    fmt.Println("string('A'):", s1)
    
    // Срез рун → строка
    runes := []rune{'П', 'р', 'и', 'в', 'е', 'т'}
    s2 := string(runes)
    fmt.Println("string(runes):", s2)
}
```

### Неизменяемость строк

```go
package main

import "fmt"

func main() {
    s := "Hello"
    
    // ❌ ОШИБКА: cannot assign to s[0]
    // s[0] = 'h'
    
    // ✅ ПРАВИЛЬНО: создаём новую строку через руны
    runes := []rune(s)
    runes[0] = 'h'
    s = string(runes)
    fmt.Println(s)  // hello
    
    // Или конкатенация
    s = "h" + s[1:]
    fmt.Println(s)  // hello
}
```

### Преобразование строки ↔ байты

```go
package main

import "fmt"

func main() {
    // Строка → байты
    s := "Привет"
    bytes := []byte(s)
    fmt.Println("Байты:", bytes)
    // [208 159 209 128 208 184 208 178 208 181 209 130]
    
    // Байты → строка
    data := []byte{72, 101, 108, 108, 111}  // ASCII коды
    str := string(data)
    fmt.Println("Строка:", str)  // Hello
}
```

### Сырые строки (raw strings)

```go
package main

import "fmt"

func main() {
    // Обычная строка — нужно экранировать
    path1 := "C:\\Users\\Name\\Documents"
    
    // Сырая строка — без экранирования
    path2 := `C:\Users\Name\Documents`
    
    fmt.Println(path1)  // C:\Users\Name\Documents
    fmt.Println(path2)  // C:\Users\Name\Documents
    
    // Многострочный текст
    html := `
<!DOCTYPE html>
<html>
    <body>Hello</body>
</html>
`
    fmt.Println(html)
}
```

### Получение символа по индексу

```go
package main

import "fmt"

func main() {
    s := "Hello, Мир!"
    
    // ❌ По байтовому индексу — некорректно для UTF-8
    fmt.Printf("s[7] = %c\n", s[7])  // Ð (часть буквы М)
    
    // ✅ Через []rune
    runes := []rune(s)
    fmt.Printf("runes[7] = %c\n", runes[7])  // М
    
    // Функция для безопасного доступа
    charAt := func(s string, index int) rune {
        return []rune(s)[index]
    }
    
    fmt.Printf("charAt(7) = %c\n", charAt(s, 7))  // М
}
```

### Конкатенация строк

```go
package main

import (
    "fmt"
    "strings"
)

func main() {
    // Способ 1: оператор +
    s1 := "Hello" + " " + "World"
    
    // Способ 2: fmt.Sprintf
    s2 := fmt.Sprintf("%s %s", "Hello", "World")
    
    // Способ 3: strings.Join (для срезов)
    parts := []string{"Hello", "World"}
    s3 := strings.Join(parts, " ")
    
    // Способ 4: strings.Builder (эффективно для многих строк)
    var builder strings.Builder
    builder.WriteString("Hello")
    builder.WriteString(" ")
    builder.WriteString("World")
    s4 := builder.String()
    
    fmt.Println(s1, s2, s3, s4)  // все "Hello World"
}
```
</Examples>

<Pitfalls>
### 1. len() возвращает байты, не символы

```go
s := "Привет"

// ❌ НЕПРАВИЛЬНО
fmt.Println("Длина:", len(s))  // 12 (байт!)

// ✅ ПРАВИЛЬНО
fmt.Println("Символов:", len([]rune(s)))  // 6
```

### 2. Доступ по индексу для UTF-8 строк

```go
s := "Привет"

// ❌ НЕПРАВИЛЬНО — получаем байт, не символ
fmt.Printf("%c\n", s[0])  // Ð (не П!)

// ✅ ПРАВИЛЬНО
fmt.Printf("%c\n", []rune(s)[0])  // П
```

### 3. Изменение строки напрямую

```go
s := "Hello"

// ❌ ОШИБКА: cannot assign to s[0]
// s[0] = 'h'

// ✅ ПРАВИЛЬНО
runes := []rune(s)
runes[0] = 'h'
s = string(runes)
```

### 4. Срез строки для UTF-8

```go
s := "Привет"

// ❌ НЕПРАВИЛЬНО — срез по байтам!
fmt.Println(s[0:2])  // "Пр" не получится, будет мусор

// ✅ ПРАВИЛЬНО — через руны
runes := []rune(s)
fmt.Println(string(runes[0:2]))  // "Пр"
```

### 5. Сравнение руны с int

```go
r := 'A'

// ❌ ПУТАНИЦА
if r == 65 {  // работает, но неочевидно
}

// ✅ ПРАВИЛЬНО — сравниваем с руной
if r == 'A' {
}
```
</Pitfalls>

<Task id="1" points="10">
<Title>Задание 1: Простой интерфейс</Title>
<Prompt>
Создайте интерфейс Speaker с методом Speak().
</Prompt>
<Hints>
- type Speaker interface { Speak() string }
- Нет ключевого слова implements
</Hints>
<StarterCode>
```go
package main

import "fmt"

// TODO: Создайте функцию согласно заданию
// TODO: Определите и реализуйте интерфейс
// TODO: Добавьте методы

func main() {
    // Ваш код здесь
    
}
```
</StarterCode>
<ExpectedOutput>
Собака: Гав!
Кошка: Мяу!
</ExpectedOutput>
</Task>

<Task id="2" points="15">
<Title>Задание 2: Полиморфизм через интерфейс</Title>
<Prompt>
Используйте один интерфейс для разных типов.
</Prompt>
<Hints>
- math.Pi для круга
- Area = width * height для прямоугольника
</Hints>
<StarterCode>
```go
package main

import "fmt"

// TODO: Создайте функцию согласно заданию
// TODO: Определите и реализуйте интерфейс

func main() {
    // Ваш код здесь
    
}
```
</StarterCode>
<ExpectedOutput>
Площадь круга: 78.54
Площадь прямоугольника: 24.00
</ExpectedOutput>
</Task>

<Task id="3" points="15">
<Title>Задание 3: Срез интерфейсов</Title>
<Prompt>
Создайте срез объектов разных типов через общий интерфейс.
</Prompt>
<Hints>
- shapes := []Shape{Rectangle{3,4}, Circle{3}}
- for _, s := range shapes {...}
</Hints>
<StarterCode>
```go
package main

import "fmt"

// TODO: Определите и реализуйте интерфейс

func main() {
    // Ваш код здесь
    
}
```
</StarterCode>
<ExpectedOutput>
Все фигуры:
- Площадь: 12.00
- Площадь: 28.27
</ExpectedOutput>
</Task>

<Task id="4" points="20">
<Title>Задание 4: Интерфейс с несколькими методами</Title>
<Prompt>
Создайте интерфейс Geometry с Area и Perimeter.
</Prompt>
<Hints>
- Тип должен реализовать ВСЕ методы
- Периметр = 2*(w+h)
</Hints>
<StarterCode>
```go
package main

import "fmt"

// TODO: Создайте функцию согласно заданию
// TODO: Определите и реализуйте интерфейс
// TODO: Добавьте методы

func main() {
    // Ваш код здесь
    
}
```
</StarterCode>
<ExpectedOutput>
Прямоугольник 5x3:
  Площадь: 15.00
  Периметр: 16.00
</ExpectedOutput>
</Task>

<Task id="5" points="20">
<Title>Задание 5: Композиция интерфейсов</Title>
<Prompt>
Создайте интерфейс из нескольких интерфейсов.
</Prompt>
<Hints>
- type ReadWriter interface { Reader; Writer }
- Встраивание интерфейсов
</Hints>
<StarterCode>
```go
package main

import (
    "bufio"
    "fmt"
    "os"
)

// TODO: Определите и реализуйте интерфейс

func main() {
    // Ваш код здесь
    
}
```
</StarterCode>
<ExpectedOutput>
ReadWriter:
  Read: Данные
  Write: OK
</ExpectedOutput>
</Task>
