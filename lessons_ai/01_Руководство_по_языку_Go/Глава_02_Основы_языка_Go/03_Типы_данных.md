# Типы данных

---

## 💡 Ключевые идеи

- Go — **статически типизированный** язык: тип определяется при компиляции
- Основные категории типов: числа, строки, логические, составные
- Типы `int` и `uint` зависят от платформы (32 или 64 бита)
- `byte` = `uint8`, `rune` = `int32` (для символов Unicode)
- Каждый тип имеет **значение по умолчанию** (zero value)

### Обзор основных типов

| Категория | Типы | Zero Value |
|-----------|------|------------|
| Целые со знаком | `int8`, `int16`, `int32`, `int64`, `int` | `0` |
| Целые без знака | `uint8`, `uint16`, `uint32`, `uint64`, `uint` | `0` |
| Дробные | `float32`, `float64` | `0.0` |
| Комплексные | `complex64`, `complex128` | `(0+0i)` |
| Логический | `bool` | `false` |
| Строка | `string` | `""` |
| Псевдонимы | `byte` (uint8), `rune` (int32) | `0` |

---

## 📖 Теория

### Зачем нужны типы данных?

Типы данных определяют:
1. **Сколько памяти** занимает значение
2. **Какие операции** допустимы
3. **Как интерпретировать** биты в памяти

Например, число `65` как `int` — это число 65. Но то же число как `byte` (символ) — это буква 'A'.

### Целые числа: со знаком и без

**Со знаком** (int8, int16, int32, int64, int):
- Могут быть отрицательными
- int8: от -128 до 127

**Без знака** (uint8, uint16, uint32, uint64, uint):
- Только положительные и ноль
- uint8: от 0 до 255

Почему такие диапазоны? Потому что один бит используется для знака. int8 имеет 8 бит, один для знака = 7 бит для значения = 2^7 = 128 значений в каждую сторону.

### int vs int32/int64

`int` — это тип, размер которого зависит от платформы:
- На 32-битной системе: int = int32
- На 64-битной системе: int = int64

Используйте `int` для большинства случаев. Используйте конкретные типы (int32, int64), когда размер важен (протоколы, файловые форматы).

### Числа с плавающей точкой

**float32** — 32 бита, ~7 значащих цифр
**float64** — 64 бита, ~15 значащих цифр

```go
var x float32 = 1.0 / 3.0  // 0.33333334
var y float64 = 1.0 / 3.0  // 0.3333333333333333
```

**Важно:** float не точен! Не используйте для денег.

```go
fmt.Println(0.1 + 0.2)  // 0.30000000000000004, не 0.3!
```

### byte и rune

**byte** — псевдоним для uint8. Используется для работы с байтами:
```go
var b byte = 65  // ASCII код буквы 'A'
```

**rune** — псевдоним для int32. Используется для символов Unicode:
```go
var r rune = 'Ы'  // Русская буква, код 1067
```

### Строки в Go

Строка (`string`) — это **неизменяемая** последовательность байтов. Важные особенности:
- Индексация возвращает **байты**, не символы
- Для русского текста один символ может занимать несколько байтов (UTF-8)

```go
s := "Привет"
fmt.Println(len(s))  // 12 (байтов), не 6 (символов)!
```

### Преобразование типов

Go не выполняет неявное преобразование типов. Вы должны делать это явно:

```go
var x int32 = 10
var y int64 = int64(x)  // явное преобразование

// var z int64 = x  // ОШИБКА!
```

Это предотвращает ошибки, связанные с потерей данных или неожиданным поведением.

---

## 📋 Синтаксис

### Целочисленные типы
```go
var a int8 = -128      // от -128 до 127
var b uint8 = 255      // от 0 до 255
var c int = 1000000    // зависит от платформы
var d byte = 65        // псевдоним для uint8
var e rune = 'A'       // псевдоним для int32 (Unicode)
```

### Числа с плавающей точкой
```go
var f float32 = 3.14   // ~6 знаков точности
var g float64 = 3.14159265358979  // ~15 знаков точности
```

### Логический тип
```go
var isActive bool = true
var isDisabled bool = false
```

### Строки
```go
var name string = "Hello, Go!"
var multiline string = `Многострочная
строка`
```

### Комплексные числа
```go
var c1 complex64 = 1 + 2i
var c2 complex128 = 3 + 4i
```

---

## 💻 Примеры кода

### Пример 1: Целочисленные типы
```go
package main

import "fmt"

func main() {
    var a int8 = 127        // максимум для int8
    var b uint8 = 255       // максимум для uint8
    var c int16 = 32767     // максимум для int16
    var d int32 = 2147483647
    var e int64 = 9223372036854775807
    var f int = 100         // рекомендуемый для большинства случаев
    
    fmt.Printf("int8: %d\n", a)
    fmt.Printf("uint8: %d\n", b)
    fmt.Printf("int16: %d\n", c)
    fmt.Printf("int32: %d\n", d)
    fmt.Printf("int64: %d\n", e)
    fmt.Printf("int: %d\n", f)
}
```

### Пример 2: byte и rune
```go
package main

import "fmt"

func main() {
    var letter byte = 'A'      // ASCII символ (uint8)
    var emoji rune = '🚀'      // Unicode символ (int32)
    
    fmt.Printf("byte 'A' = %d, символ: %c\n", letter, letter)
    fmt.Printf("rune '🚀' = %d, символ: %c\n", emoji, emoji)
    
    // Строка как набор byte
    str := "Hi"
    fmt.Printf("str[0] = %c, str[1] = %c\n", str[0], str[1])
}
```

### Пример 3: Числа с плавающей точкой
```go
package main

import "fmt"

func main() {
    var pi32 float32 = 3.14159265358979323846
    var pi64 float64 = 3.14159265358979323846
    
    fmt.Printf("float32: %.20f\n", pi32)  // потеря точности
    fmt.Printf("float64: %.20f\n", pi64)  // больше точности
    
    // Научная нотация
    var big = 1.5e10    // 1.5 × 10^10
    var small = 2.5e-5  // 2.5 × 10^-5
    
    fmt.Printf("big: %e\n", big)
    fmt.Printf("small: %e\n", small)
}
```

### Пример 4: Логический тип
```go
package main

import "fmt"

func main() {
    var isGo bool = true
    var isPython bool = false
    
    fmt.Printf("Go: %t, Python: %t\n", isGo, isPython)
    
    // Результат сравнения — bool
    x := 10
    y := 20
    isGreater := x > y
    fmt.Printf("%d > %d = %t\n", x, y, isGreater)
}
```

### Пример 5: Строки
```go
package main

import "fmt"

func main() {
    // Обычная строка (интерпретируемая)
    greeting := "Hello\nWorld"  // \n — перенос строки
    
    // Сырая строка (raw string)
    path := `C:\Users\name\file.txt`  // \ не экранируется
    
    // Многострочная
    poem := `Розы красные,
Фиалки синие,
Go — отличный,
И ты тоже.`
    
    fmt.Println(greeting)
    fmt.Println(path)
    fmt.Println(poem)
}
```

### Пример 6: Zero values (значения по умолчанию)
```go
package main

import "fmt"

func main() {
    var i int
    var f float64
    var b bool
    var s string
    var r rune
    
    fmt.Printf("int: %d\n", i)       // 0
    fmt.Printf("float64: %f\n", f)   // 0.000000
    fmt.Printf("bool: %t\n", b)      // false
    fmt.Printf("string: %q\n", s)    // ""
    fmt.Printf("rune: %d\n", r)      // 0
}
```

### Пример 7: Вывод типа (автоматический выбор)
```go
package main

import "fmt"

func main() {
    a := 42          // int (не int32!)
    b := 3.14        // float64 (не float32!)
    c := true        // bool
    d := "hello"     // string
    e := 'A'         // rune (не byte!)
    
    fmt.Printf("a: %T = %v\n", a, a)
    fmt.Printf("b: %T = %v\n", b, b)
    fmt.Printf("c: %T = %v\n", c, c)
    fmt.Printf("d: %T = %v\n", d, d)
    fmt.Printf("e: %T = %v\n", e, e)
}
```

### Пример 8: Размеры типов
```go
package main

import (
    "fmt"
    "unsafe"
)

func main() {
    var i8 int8
    var i16 int16
    var i32 int32
    var i64 int64
    var i int
    
    fmt.Printf("int8:  %d байт\n", unsafe.Sizeof(i8))   // 1
    fmt.Printf("int16: %d байт\n", unsafe.Sizeof(i16))  // 2
    fmt.Printf("int32: %d байт\n", unsafe.Sizeof(i32))  // 4
    fmt.Printf("int64: %d байт\n", unsafe.Sizeof(i64))  // 8
    fmt.Printf("int:   %d байт\n", unsafe.Sizeof(i))    // 4 или 8
}
```

---

## ⚠️ Частые ошибки

### 1. Смешивание разных числовых типов
```go
// ❌ Неправильно — Go не делает автоматическое приведение
var a int32 = 10
var b int64 = 20
var c = a + b  // invalid operation: mismatched types

// ✅ Правильно — явное преобразование
var a int32 = 10
var b int64 = 20
var c = int64(a) + b  // 30
```

### 2. Переполнение типа
```go
// ❌ Неправильно — значение вне диапазона
var a int8 = 128  // constant 128 overflows int8 (max 127)

// ✅ Правильно
var a int8 = 127
// или используйте больший тип
var b int16 = 128
```

### 3. Потеря точности float32 vs float64
```go
// ⚠️ Осторожно с float32
var f32 float32 = 0.1
var f64 float64 = 0.1

fmt.Printf("float32: %.20f\n", f32)  // 0.10000000149011611938
fmt.Printf("float64: %.20f\n", f64)  // 0.10000000000000000555

// Рекомендация: используйте float64 по умолчанию
```

### 4. Путаница byte и rune
```go
// ❌ Неправильно — Unicode символ не влезет в byte
var emoji byte = '🚀'  // constant 128640 overflows byte

// ✅ Правильно
var emoji rune = '🚀'
var letter byte = 'A'  // ASCII символы — OK
```

### 5. Сравнение float с точным равенством
```go
// ❌ Опасно — float может иметь погрешность
var a float64 = 0.1 + 0.2
if a == 0.3 {  // может не сработать!
    fmt.Println("Равно")
}

// ✅ Правильно — сравнение с погрешностью
const epsilon = 0.0000001
if math.Abs(a - 0.3) < epsilon {
    fmt.Println("Примерно равно")
}
```

### 6. Строки неизменяемы
```go
// ❌ Неправильно — нельзя изменить символ в строке
s := "hello"
s[0] = 'H'  // cannot assign to s[0]

// ✅ Правильно — создайте новую строку
s := "hello"
s = "H" + s[1:]  // "Hello"
// или через []byte
b := []byte(s)
b[0] = 'H'
s = string(b)
```

---

## 🔗 Полезные ссылки

- [A Tour of Go — Basic Types](https://go.dev/tour/basics/11)
- [Go Spec — Types](https://go.dev/ref/spec#Types)
- [Unicode в Go](https://go.dev/blog/strings)

---

## 🏋️ Практические задания

### Задание 1: Целочисленные типы

Создайте переменные разных целочисленных типов и выведите их значения и размеры в байтах.

**Ожидаемый результат:**
```
int8: 127 (макс для int8)
int16: 32767 (макс для int16)
int32: 2147483647 (макс для int32)
```

**Критерии приёмки:**
- Созданы переменные типов int8, int16, int32
- Значения — максимальные для каждого типа
- Выведен тип и значение каждой переменной

**Подсказки:**
- int8: от -128 до 127
- int16: от -32768 до 32767
- int32: от -2147483648 до 2147483647

**Начальный код:**
```go
package main

import "fmt"

func main() {
    // Создайте переменные разных типов
    var a int8 = 127
    // Добавьте int16 и int32
}
```

**Ожидаемый вывод:**
```
int8: 127 (макс для int8)
int16: 32767 (макс для int16)
int32: 2147483647 (макс для int32)
```

**Баллы:** 10

### Задание 2: Числа с плавающей точкой

Создайте переменные float32 и float64 для хранения числа π (Pi). Выведите их с разной точностью.

**Ожидаемый результат:**
```
float32 Pi: 3.1416
float64 Pi: 3.14159265358979
```

**Критерии приёмки:**
- Создана переменная float32 с приближённым значением Pi
- Создана переменная float64 с более точным значением
- float32 выведен с 4 знаками после запятой
- float64 выведен с 14 знаками после запятой

**Подсказки:**
- Pi ≈ 3.14159265358979
- Используйте `%.4f` и `%.14f` для форматирования

**Начальный код:**
```go
package main

import "fmt"

func main() {
    var pi32 float32 = 3.14159265
    var pi64 float64 = 3.14159265358979
    // Выведите с разной точностью
}
```

**Ожидаемый вывод:**
```
float32 Pi: 3.1416
float64 Pi: 3.14159265358979
```

**Баллы:** 10

### Задание 3: Логический тип bool

Создайте логические переменные для проверки условий и выведите результаты логических операций.

**Ожидаемый результат:**
```
isGo: true
isPython: false
isGo AND isPython: false
isGo OR isPython: true
NOT isGo: false
```

**Критерии приёмки:**
- Созданы две bool переменные
- Выполнены операции AND (&&), OR (||), NOT (!)
- Все результаты выведены корректно

**Подсказки:**
- `&&` — логическое И
- `||` — логическое ИЛИ
- `!` — логическое НЕ

**Начальный код:**
```go
package main

import "fmt"

func main() {
    isGo := true
    isPython := false
    // Выведите переменные и результаты операций
}
```

**Ожидаемый вывод:**
```
isGo: true
isPython: false
isGo AND isPython: false
isGo OR isPython: true
NOT isGo: false
```

**Баллы:** 15

### Задание 4: Строки и руны

Работайте со строкой на кириллице. Выведите её длину в байтах и количество символов (рун).

**Ожидаемый результат:**
```
Строка: Привет
Байт: 12
Символов: 6
Первая руна: П
```

**Критерии приёмки:**
- Строка содержит кириллицу "Привет"
- Выведена длина в байтах через `len()`
- Выведено количество рун через `utf8.RuneCountInString()`
- Выведена первая руна строки

**Подсказки:**
- Кириллический символ занимает 2 байта в UTF-8
- Импортируйте `"unicode/utf8"` для работы с рунами

**Начальный код:**
```go
package main

import (
    "fmt"
    "unicode/utf8"
)

func main() {
    s := "Привет"
    // Выведите информацию о строке
}
```

**Ожидаемый вывод:**
```
Строка: Привет
Байт: 12
Символов: 6
Первая руна: П
```

**Баллы:** 15

### Задание 5: Преобразование типов

Продемонстрируйте явное преобразование между типами: int → float64 и float64 → int. Покажите потерю точности при преобразовании.

**Ожидаемый результат:**
```
Целое 42 -> вещественное: 42.00
Вещественное 3.99 -> целое: 3
Вещественное 3.14 -> целое: 3 (потеря: 0.14)
```

**Критерии приёмки:**
- Преобразование int в float64 с выводом
- Преобразование float64 в int с выводом
- Показана потеря дробной части при преобразовании в int
- Используется явное приведение типов

**Подсказки:**
- `float64(intVar)` — преобразует int в float64
- `int(floatVar)` — отбрасывает дробную часть (не округляет!)

**Начальный код:**
```go
package main

import "fmt"

func main() {
    i := 42
    f1 := 3.99
    f2 := 3.14
    // Выполните преобразования и выведите результаты
}
```

**Ожидаемый вывод:**
```
Целое 42 -> вещественное: 42.00
Вещественное 3.99 -> целое: 3
Вещественное 3.14 -> целое: 3 (потеря: 0.14)
```

**Баллы:** 20
