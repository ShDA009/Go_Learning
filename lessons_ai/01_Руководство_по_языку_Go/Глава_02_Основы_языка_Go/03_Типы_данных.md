# Типы данных

---

## 💡 Ключевые идеи

- Go — **статически типизированный** язык: тип определяется при компиляции
- Основные категории типов: числа, строки, логические, составные
- Типы `int` и `uint` зависят от платформы (32 или 64 бита)
- `byte` = `uint8`, `rune` = `int32` (для символов Unicode)
- Каждый тип имеет **значение по умолчанию** (zero value)

### Обзор основных типов

| Категория | Типы | Zero Value |
|-----------|------|------------|
| Целые со знаком | `int8`, `int16`, `int32`, `int64`, `int` | `0` |
| Целые без знака | `uint8`, `uint16`, `uint32`, `uint64`, `uint` | `0` |
| Дробные | `float32`, `float64` | `0.0` |
| Комплексные | `complex64`, `complex128` | `(0+0i)` |
| Логический | `bool` | `false` |
| Строка | `string` | `""` |
| Псевдонимы | `byte` (uint8), `rune` (int32) | `0` |

---

## 📋 Синтаксис

### Целочисленные типы
```go
var a int8 = -128      // от -128 до 127
var b uint8 = 255      // от 0 до 255
var c int = 1000000    // зависит от платформы
var d byte = 65        // псевдоним для uint8
var e rune = 'A'       // псевдоним для int32 (Unicode)
```

### Числа с плавающей точкой
```go
var f float32 = 3.14   // ~6 знаков точности
var g float64 = 3.14159265358979  // ~15 знаков точности
```

### Логический тип
```go
var isActive bool = true
var isDisabled bool = false
```

### Строки
```go
var name string = "Hello, Go!"
var multiline string = `Многострочная
строка`
```

### Комплексные числа
```go
var c1 complex64 = 1 + 2i
var c2 complex128 = 3 + 4i
```

---

## 💻 Примеры кода

### Пример 1: Целочисленные типы
```go
package main

import "fmt"

func main() {
    var a int8 = 127        // максимум для int8
    var b uint8 = 255       // максимум для uint8
    var c int16 = 32767     // максимум для int16
    var d int32 = 2147483647
    var e int64 = 9223372036854775807
    var f int = 100         // рекомендуемый для большинства случаев
    
    fmt.Printf("int8: %d\n", a)
    fmt.Printf("uint8: %d\n", b)
    fmt.Printf("int16: %d\n", c)
    fmt.Printf("int32: %d\n", d)
    fmt.Printf("int64: %d\n", e)
    fmt.Printf("int: %d\n", f)
}
```

### Пример 2: byte и rune
```go
package main

import "fmt"

func main() {
    var letter byte = 'A'      // ASCII символ (uint8)
    var emoji rune = '🚀'      // Unicode символ (int32)
    
    fmt.Printf("byte 'A' = %d, символ: %c\n", letter, letter)
    fmt.Printf("rune '🚀' = %d, символ: %c\n", emoji, emoji)
    
    // Строка как набор byte
    str := "Hi"
    fmt.Printf("str[0] = %c, str[1] = %c\n", str[0], str[1])
}
```

### Пример 3: Числа с плавающей точкой
```go
package main

import "fmt"

func main() {
    var pi32 float32 = 3.14159265358979323846
    var pi64 float64 = 3.14159265358979323846
    
    fmt.Printf("float32: %.20f\n", pi32)  // потеря точности
    fmt.Printf("float64: %.20f\n", pi64)  // больше точности
    
    // Научная нотация
    var big = 1.5e10    // 1.5 × 10^10
    var small = 2.5e-5  // 2.5 × 10^-5
    
    fmt.Printf("big: %e\n", big)
    fmt.Printf("small: %e\n", small)
}
```

### Пример 4: Логический тип
```go
package main

import "fmt"

func main() {
    var isGo bool = true
    var isPython bool = false
    
    fmt.Printf("Go: %t, Python: %t\n", isGo, isPython)
    
    // Результат сравнения — bool
    x := 10
    y := 20
    isGreater := x > y
    fmt.Printf("%d > %d = %t\n", x, y, isGreater)
}
```

### Пример 5: Строки
```go
package main

import "fmt"

func main() {
    // Обычная строка (интерпретируемая)
    greeting := "Hello\nWorld"  // \n — перенос строки
    
    // Сырая строка (raw string)
    path := `C:\Users\name\file.txt`  // \ не экранируется
    
    // Многострочная
    poem := `Розы красные,
Фиалки синие,
Go — отличный,
И ты тоже.`
    
    fmt.Println(greeting)
    fmt.Println(path)
    fmt.Println(poem)
}
```

### Пример 6: Zero values (значения по умолчанию)
```go
package main

import "fmt"

func main() {
    var i int
    var f float64
    var b bool
    var s string
    var r rune
    
    fmt.Printf("int: %d\n", i)       // 0
    fmt.Printf("float64: %f\n", f)   // 0.000000
    fmt.Printf("bool: %t\n", b)      // false
    fmt.Printf("string: %q\n", s)    // ""
    fmt.Printf("rune: %d\n", r)      // 0
}
```

### Пример 7: Вывод типа (автоматический выбор)
```go
package main

import "fmt"

func main() {
    a := 42          // int (не int32!)
    b := 3.14        // float64 (не float32!)
    c := true        // bool
    d := "hello"     // string
    e := 'A'         // rune (не byte!)
    
    fmt.Printf("a: %T = %v\n", a, a)
    fmt.Printf("b: %T = %v\n", b, b)
    fmt.Printf("c: %T = %v\n", c, c)
    fmt.Printf("d: %T = %v\n", d, d)
    fmt.Printf("e: %T = %v\n", e, e)
}
```

### Пример 8: Размеры типов
```go
package main

import (
    "fmt"
    "unsafe"
)

func main() {
    var i8 int8
    var i16 int16
    var i32 int32
    var i64 int64
    var i int
    
    fmt.Printf("int8:  %d байт\n", unsafe.Sizeof(i8))   // 1
    fmt.Printf("int16: %d байт\n", unsafe.Sizeof(i16))  // 2
    fmt.Printf("int32: %d байт\n", unsafe.Sizeof(i32))  // 4
    fmt.Printf("int64: %d байт\n", unsafe.Sizeof(i64))  // 8
    fmt.Printf("int:   %d байт\n", unsafe.Sizeof(i))    // 4 или 8
}
```

---

## ⚠️ Частые ошибки

### 1. Смешивание разных числовых типов
```go
// ❌ Неправильно — Go не делает автоматическое приведение
var a int32 = 10
var b int64 = 20
var c = a + b  // invalid operation: mismatched types

// ✅ Правильно — явное преобразование
var a int32 = 10
var b int64 = 20
var c = int64(a) + b  // 30
```

### 2. Переполнение типа
```go
// ❌ Неправильно — значение вне диапазона
var a int8 = 128  // constant 128 overflows int8 (max 127)

// ✅ Правильно
var a int8 = 127
// или используйте больший тип
var b int16 = 128
```

### 3. Потеря точности float32 vs float64
```go
// ⚠️ Осторожно с float32
var f32 float32 = 0.1
var f64 float64 = 0.1

fmt.Printf("float32: %.20f\n", f32)  // 0.10000000149011611938
fmt.Printf("float64: %.20f\n", f64)  // 0.10000000000000000555

// Рекомендация: используйте float64 по умолчанию
```

### 4. Путаница byte и rune
```go
// ❌ Неправильно — Unicode символ не влезет в byte
var emoji byte = '🚀'  // constant 128640 overflows byte

// ✅ Правильно
var emoji rune = '🚀'
var letter byte = 'A'  // ASCII символы — OK
```

### 5. Сравнение float с точным равенством
```go
// ❌ Опасно — float может иметь погрешность
var a float64 = 0.1 + 0.2
if a == 0.3 {  // может не сработать!
    fmt.Println("Равно")
}

// ✅ Правильно — сравнение с погрешностью
const epsilon = 0.0000001
if math.Abs(a - 0.3) < epsilon {
    fmt.Println("Примерно равно")
}
```

### 6. Строки неизменяемы
```go
// ❌ Неправильно — нельзя изменить символ в строке
s := "hello"
s[0] = 'H'  // cannot assign to s[0]

// ✅ Правильно — создайте новую строку
s := "hello"
s = "H" + s[1:]  // "Hello"
// или через []byte
b := []byte(s)
b[0] = 'H'
s = string(b)
```

---

## 📝 Практика

### Задание 1: Объявите переменные всех типов
Создайте переменные каждого базового типа и выведите их значения и типы:
- целое число
- дробное число
- логическое значение
- строка
- символ (rune)

<details>
<summary>Решение</summary>

```go
package main

import "fmt"

func main() {
    var integer int = 42
    var floating float64 = 3.14159
    var boolean bool = true
    var text string = "Hello, Go!"
    var character rune = 'G'
    
    fmt.Printf("integer: %v (тип: %T)\n", integer, integer)
    fmt.Printf("floating: %v (тип: %T)\n", floating, floating)
    fmt.Printf("boolean: %v (тип: %T)\n", boolean, boolean)
    fmt.Printf("text: %v (тип: %T)\n", text, text)
    fmt.Printf("character: %c (тип: %T)\n", character, character)
}
```
</details>

### Задание 2: Преобразование типов
Преобразуйте число 65 в символ и выведите его. Преобразуйте float64 в int.

<details>
<summary>Решение</summary>

```go
package main

import "fmt"

func main() {
    // Число в символ
    code := 65
    char := rune(code)
    fmt.Printf("Код %d = символ '%c'\n", code, char)
    
    // float64 в int (отбрасывает дробную часть)
    pi := 3.14159
    intPi := int(pi)
    fmt.Printf("float64 %.5f -> int %d\n", pi, intPi)
}
```
</details>

### Задание 3: Escape-последовательности
Выведите строку, содержащую:
- перенос строки
- табуляцию
- кавычки
- обратный слеш

<details>
<summary>Решение</summary>

```go
package main

import "fmt"

func main() {
    s := "Строка 1\nСтрока 2\n\tС табуляцией\nСо \"кавычками\" и обратным слешем: \\"
    fmt.Println(s)
}
```
</details>

### Задание 4: Вычислите размер
Используя `unsafe.Sizeof`, выведите размер всех целочисленных типов на вашей машине.

<details>
<summary>Решение</summary>

```go
package main

import (
    "fmt"
    "unsafe"
)

func main() {
    fmt.Printf("int:    %d байт\n", unsafe.Sizeof(int(0)))
    fmt.Printf("int8:   %d байт\n", unsafe.Sizeof(int8(0)))
    fmt.Printf("int16:  %d байт\n", unsafe.Sizeof(int16(0)))
    fmt.Printf("int32:  %d байт\n", unsafe.Sizeof(int32(0)))
    fmt.Printf("int64:  %d байт\n", unsafe.Sizeof(int64(0)))
    fmt.Printf("uint:   %d байт\n", unsafe.Sizeof(uint(0)))
    fmt.Printf("byte:   %d байт\n", unsafe.Sizeof(byte(0)))
    fmt.Printf("rune:   %d байт\n", unsafe.Sizeof(rune(0)))
}
```
</details>

### Задание 5: Работа с rune
Выведите Unicode-коды и символы для: A, Я, 🎉

<details>
<summary>Решение</summary>

```go
package main

import "fmt"

func main() {
    chars := []rune{'A', 'Я', '🎉'}
    
    for _, ch := range chars {
        fmt.Printf("Символ: %c, Код: %d, Hex: %X\n", ch, ch, ch)
    }
}
```
</details>

---

## 🔗 Полезные ссылки

- [A Tour of Go — Basic Types](https://go.dev/tour/basics/11)
- [Go Spec — Types](https://go.dev/ref/spec#Types)
- [Unicode в Go](https://go.dev/blog/strings)
