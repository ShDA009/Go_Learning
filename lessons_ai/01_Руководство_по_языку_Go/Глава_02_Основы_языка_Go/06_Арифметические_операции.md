# Арифметические операции

---

## 💡 Ключевые идеи

- Go поддерживает стандартные арифметические операции: `+`, `-`, `*`, `/`, `%`
- **Деление целых чисел** отбрасывает дробную часть
- `%` (остаток от деления) работает **только с целыми числами**
- `++` и `--` — только **постфиксные** и только как **отдельные инструкции**
- Операции между **разными типами** требуют явного преобразования

### Таблица арифметических операций

| Операция | Символ | Пример | Результат |
|----------|--------|--------|-----------|
| Сложение | `+` | `5 + 3` | `8` |
| Вычитание | `-` | `5 - 3` | `2` |
| Умножение | `*` | `5 * 3` | `15` |
| Деление | `/` | `5 / 3` | `1` (для int) |
| Остаток | `%` | `5 % 3` | `2` |
| Инкремент | `++` | `x++` | `x = x + 1` |
| Декремент | `--` | `x--` | `x = x - 1` |

---

## 📋 Синтаксис

### Базовые операции
```go
a + b   // сложение
a - b   // вычитание
a * b   // умножение
a / b   // деление
a % b   // остаток от деления (только для целых)
```

### Инкремент и декремент
```go
x++     // x = x + 1 (постфиксный)
x--     // x = x - 1 (постфиксный)
```

### Унарный минус
```go
-x      // смена знака
```

---

## 💻 Примеры кода

### Пример 1: Базовые операции
```go
package main

import "fmt"

func main() {
    a := 10
    b := 3
    
    fmt.Printf("%d + %d = %d\n", a, b, a+b)   // 10 + 3 = 13
    fmt.Printf("%d - %d = %d\n", a, b, a-b)   // 10 - 3 = 7
    fmt.Printf("%d * %d = %d\n", a, b, a*b)   // 10 * 3 = 30
    fmt.Printf("%d / %d = %d\n", a, b, a/b)   // 10 / 3 = 3
    fmt.Printf("%d %% %d = %d\n", a, b, a%b)  // 10 % 3 = 1
}
```

### Пример 2: Деление целых vs дробных
```go
package main

import "fmt"

func main() {
    // Деление целых чисел — результат целый
    intA := 10
    intB := 3
    fmt.Printf("int: %d / %d = %d\n", intA, intB, intA/intB)  // 3
    
    // Деление дробных — результат дробный
    floatA := 10.0
    floatB := 3.0
    fmt.Printf("float: %.1f / %.1f = %.4f\n", floatA, floatB, floatA/floatB)  // 3.3333
    
    // Смешанное деление — нужно преобразование!
    result := float64(intA) / float64(intB)
    fmt.Printf("mixed: %d / %d = %.4f\n", intA, intB, result)  // 3.3333
}
```

### Пример 3: Остаток от деления
```go
package main

import "fmt"

func main() {
    // Проверка чётности
    for i := 0; i < 10; i++ {
        if i%2 == 0 {
            fmt.Printf("%d — чётное\n", i)
        } else {
            fmt.Printf("%d — нечётное\n", i)
        }
    }
    
    // Циклический индекс
    colors := []string{"red", "green", "blue"}
    for i := 0; i < 10; i++ {
        fmt.Printf("i=%d, color=%s\n", i, colors[i%len(colors)])
    }
}
```

### Пример 4: Инкремент и декремент
```go
package main

import "fmt"

func main() {
    x := 5
    
    x++
    fmt.Println("После x++:", x)  // 6
    
    x++
    fmt.Println("После x++:", x)  // 7
    
    x--
    fmt.Println("После x--:", x)  // 6
}
```

### Пример 5: Унарный минус
```go
package main

import "fmt"

func main() {
    a := 5
    b := -a     // унарный минус
    c := -(-a)  // двойное отрицание
    
    fmt.Println("a =", a)   // 5
    fmt.Println("b =", b)   // -5
    fmt.Println("c =", c)   // 5
    
    // Вычисление модуля (абсолютного значения)
    x := -10
    if x < 0 {
        x = -x
    }
    fmt.Println("|x| =", x)  // 10
}
```

### Пример 6: Порядок операций (приоритет)
```go
package main

import "fmt"

func main() {
    // Умножение/деление выполняются раньше сложения/вычитания
    a := 2 + 3 * 4      // 2 + 12 = 14
    b := (2 + 3) * 4    // 5 * 4 = 20
    
    fmt.Println("2 + 3 * 4 =", a)     // 14
    fmt.Println("(2 + 3) * 4 =", b)   // 20
    
    // Деление и остаток — одинаковый приоритет, слева направо
    c := 20 / 4 % 3     // 5 % 3 = 2
    d := 20 % 4 / 3     // 0 / 3 = 0
    
    fmt.Println("20 / 4 % 3 =", c)    // 2
    fmt.Println("20 % 4 / 3 =", d)    // 0
}
```

### Пример 7: Практические примеры
```go
package main

import "fmt"

func main() {
    // Перевод секунд в минуты и секунды
    totalSeconds := 185
    minutes := totalSeconds / 60
    seconds := totalSeconds % 60
    fmt.Printf("%d секунд = %d мин %d сек\n", totalSeconds, minutes, seconds)
    
    // Расчёт среднего
    sum := 85 + 90 + 78 + 92 + 88
    count := 5
    average := float64(sum) / float64(count)
    fmt.Printf("Средний балл: %.2f\n", average)
    
    // Расчёт скидки
    price := 1000.0
    discount := 15  // процентов
    finalPrice := price - price*float64(discount)/100
    fmt.Printf("Цена со скидкой %d%%: %.2f\n", discount, finalPrice)
}
```

### Пример 8: Переполнение (overflow)
```go
package main

import "fmt"

func main() {
    var a int8 = 127
    fmt.Println("int8 max:", a)
    
    // a++ вызовет переполнение!
    a++
    fmt.Println("После ++:", a)  // -128 (переполнение!)
    
    // Безопасная проверка
    var b int8 = 127
    if b < 127 {
        b++
    } else {
        fmt.Println("Нельзя увеличить — переполнение!")
    }
}
```

---

## ⚠️ Частые ошибки

### 1. Деление целых — потеря дробной части
```go
// ❌ Неправильно — ожидали 2.5
result := 5 / 2  // result = 2, а не 2.5!

// ✅ Правильно — используйте float
result := 5.0 / 2.0  // 2.5
// или преобразование
result := float64(5) / float64(2)  // 2.5
```

### 2. Остаток от деления с float
```go
// ❌ Неправильно — % не работает с float
var a, b float64 = 5.5, 2.0
result := a % b  // invalid operation

// ✅ Правильно — используйте math.Mod
import "math"
result := math.Mod(a, b)  // 1.5
```

### 3. Префиксный ++/--
```go
// ❌ Неправильно — в Go нет префиксного инкремента
x := 5
y := ++x  // syntax error

// ✅ Правильно — только постфиксный и отдельно
x := 5
x++
y := x
```

### 4. Инкремент как выражение
```go
// ❌ Неправильно — нельзя использовать в выражении
x := 5
y := x++ + 1  // syntax error

// ✅ Правильно — инкремент отдельно
x := 5
x++
y := x + 1
```

### 5. Операции с разными типами
```go
// ❌ Неправильно — разные типы
var a int = 10
var b int64 = 20
c := a + b  // invalid operation: mismatched types

// ✅ Правильно — явное преобразование
c := int64(a) + b  // OK
```

### 6. Деление на ноль
```go
// ❌ Деление целого на ноль — паника!
a := 10
b := 0
c := a / b  // panic: runtime error: integer divide by zero

// ✅ Проверяйте делитель
if b != 0 {
    c := a / b
}

// Примечание: для float — результат Inf или NaN
var f float64 = 10.0 / 0.0  // +Inf (не паника)
```

---

## 📝 Практика

### Задание 1: Калькулятор
Напишите программу, которая для чисел 17 и 5 выводит результаты всех арифметических операций.

<details>
<summary>Решение</summary>

```go
package main

import "fmt"

func main() {
    a, b := 17, 5
    
    fmt.Printf("%d + %d = %d\n", a, b, a+b)
    fmt.Printf("%d - %d = %d\n", a, b, a-b)
    fmt.Printf("%d * %d = %d\n", a, b, a*b)
    fmt.Printf("%d / %d = %d\n", a, b, a/b)
    fmt.Printf("%d %% %d = %d\n", a, b, a%b)
}
```
</details>

### Задание 2: Конвертер времени
Преобразуйте 3725 секунд в часы, минуты и секунды.

<details>
<summary>Решение</summary>

```go
package main

import "fmt"

func main() {
    total := 3725
    
    hours := total / 3600
    minutes := (total % 3600) / 60
    seconds := total % 60
    
    fmt.Printf("%d секунд = %d ч %d мин %d сек\n", 
               total, hours, minutes, seconds)
    // 3725 секунд = 1 ч 2 мин 5 сек
}
```
</details>

### Задание 3: Средний балл
Вычислите средний балл для оценок: 4, 5, 3, 5, 4 с точностью до двух знаков.

<details>
<summary>Решение</summary>

```go
package main

import "fmt"

func main() {
    grades := []int{4, 5, 3, 5, 4}
    sum := 0
    
    for _, grade := range grades {
        sum += grade
    }
    
    average := float64(sum) / float64(len(grades))
    fmt.Printf("Средний балл: %.2f\n", average)  // 4.20
}
```
</details>

### Задание 4: Расчёт стоимости
Товар стоит 1599 рублей. Рассчитайте цену со скидкой 20% и сумму скидки.

<details>
<summary>Решение</summary>

```go
package main

import "fmt"

func main() {
    price := 1599.0
    discountPercent := 20
    
    discount := price * float64(discountPercent) / 100
    finalPrice := price - discount
    
    fmt.Printf("Начальная цена: %.2f руб\n", price)
    fmt.Printf("Скидка %d%%: %.2f руб\n", discountPercent, discount)
    fmt.Printf("Итого: %.2f руб\n", finalPrice)
}
```
</details>

### Задание 5: Разбиение числа на цифры
Для числа 1234 выведите каждую цифру отдельно (используя деление и остаток).

<details>
<summary>Решение</summary>

```go
package main

import "fmt"

func main() {
    n := 1234
    
    // Извлекаем цифры справа налево
    d1 := n % 10         // 4
    d2 := (n / 10) % 10  // 3
    d3 := (n / 100) % 10 // 2
    d4 := n / 1000       // 1
    
    fmt.Printf("Число: %d\n", n)
    fmt.Printf("Цифры: %d, %d, %d, %d\n", d4, d3, d2, d1)
    
    // Или в цикле
    fmt.Print("Цифры (цикл): ")
    temp := n
    for temp > 0 {
        digit := temp % 10
        fmt.Printf("%d ", digit)
        temp /= 10
    }
    fmt.Println()
}
```
</details>

---

## 🔗 Полезные ссылки

- [Go Spec — Arithmetic operators](https://go.dev/ref/spec#Arithmetic_operators)
- [Пакет math](https://pkg.go.dev/math)
