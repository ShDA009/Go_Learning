# Что такое Go

---

## 💡 Ключевые идеи

- **Go (Golang)** — компилируемый, статически типизированный язык программирования от Google
- Создан в 2007 году, публично представлен в 2009, версия 1.0 вышла в 2012
- Основные авторы: Роберт Гриземер, Роб Пайк, Кен Томпсон (один из создателей языка C)
- **Главные преимущества**: простота, скорость компиляции, встроенная поддержка параллелизма
- Идеален для: веб-сервисов, микросервисов, CLI-инструментов, системного программирования

### Ключевые особенности языка

| Особенность | Описание |
|-------------|----------|
| **Компилируемый** | Код компилируется в машинный код, выполняется без runtime |
| **Статическая типизация** | Типы проверяются на этапе компиляции |
| **Сборка мусора** | Автоматическое управление памятью (GC mark-and-sweep) |
| **Горутины** | Легковесные потоки для параллельного выполнения |
| **Каналы** | Безопасный обмен данными между горутинами |
| **Generics** | Обобщённые типы (с версии 1.18) |
| **Кроссплатформенность** | Windows, Linux, macOS, FreeBSD, ARM |

---

## 📖 Теория

### История создания Go

В 2007 году три инженера Google — **Роберт Гриземер**, **Роб Пайк** и **Кен Томпсон** — были недовольны существующими языками программирования. C++ был слишком сложным, Java требовала много boilerplate-кода, Python был медленным. Они решили создать новый язык, который объединит лучшие качества.

Кен Томпсон — легенда программирования, один из создателей Unix и языка C. Роб Пайк работал над операционной системой Plan 9. Их опыт позволил создать язык, который решает реальные проблемы разработки.

### Почему Go называют "простым"?

Go намеренно **минималистичен**. В языке нет:
- Наследования классов (только композиция)
- Исключений (только явная обработка ошибок)
- Generics (до версии 1.18)
- Перегрузки функций
- Неявных преобразований типов

Это не ограничение — это философия. Меньше способов сделать одно и то же = проще читать чужой код.

### Как Go достигает высокой производительности?

**1. Компиляция в машинный код:**
Go компилируется напрямую в машинный код, без промежуточного байт-кода (как в Java) или интерпретации (как в Python).

**2. Быстрая компиляция:**
Go компилирует миллион строк кода за секунды. Это достигается за счёт:
- Простого синтаксиса
- Отсутствия заголовочных файлов
- Умного анализа зависимостей

**3. Эффективный сборщик мусора:**
GC в Go оптимизирован для низких пауз (обычно < 1ms).

### Горутины — главная фишка Go

**Горутина** — это легковесный поток выполнения. В отличие от системных потоков (threads):
- Создание горутины занимает ~2KB памяти (thread ~1MB)
- Можно создать сотни тысяч горутин
- Планировщик Go сам распределяет их по CPU-ядрам

```go
go функция()  // Запустить функцию как горутину
```

### Где используется Go?

- **Docker** — контейнеризация
- **Kubernetes** — оркестрация контейнеров
- **Terraform** — инфраструктура как код
- **Prometheus** — мониторинг
- **Hugo** — генератор статических сайтов
- **Caddy** — веб-сервер

Если вы работаете с DevOps, облаками или микросервисами — Go везде.

---

## 📋 Синтаксис

### Структура имени файла
```
имя_файла.go
```

### Минимальная программа
```go
package main  // Обязательно для исполняемых программ

func main() {
    // Точка входа в программу
}
```

### Проверка версии Go
```bash
go version
```

---

## 💻 Примеры кода

### Проверка установки Go
```bash
# Проверить версию
go version
# Вывод: go version go1.22.0 windows/amd64

# Посмотреть переменные окружения Go
go env

# Проверить путь установки
go env GOROOT
```

### Hello World (минимальный пример)
```go
package main

import "fmt"

func main() {
    fmt.Println("Привет, Go!")
}
```

### Запуск и компиляция
```bash
# Запустить без компиляции в файл
go run main.go

# Скомпилировать в исполняемый файл
go build main.go

# Запустить скомпилированный файл
./main        # Linux/macOS
main.exe      # Windows
```

---

## ⚠️ Частые ошибки

### 1. Go не найден в PATH
```bash
# Ошибка
'go' is not recognized as an internal or external command

# Решение: добавить Go в PATH
# Windows: добавить C:\Go\bin в системные переменные
# Linux/Mac: добавить export PATH=$PATH:/usr/local/go/bin в ~/.bashrc
```

### 2. Неправильная версия Go
```bash
# Проверить версию
go version

# Если версия старая — скачать новую с https://go.dev/dl/
```

### 3. GOPATH vs Go Modules
```bash
# Старый подход (до Go 1.11) — использование GOPATH
# Новый подход (с Go 1.11+) — Go Modules (рекомендуется)

# Инициализация модуля
go mod init имя_модуля
```

### 4. Ошибка "package main is not a main package"
```go
// ❌ Неправильно - файл без package main нельзя запустить напрямую
package utils

// ✅ Правильно - исполняемая программа
package main
```

---

## 🔗 Полезные ссылки

- [Официальный сайт Go](https://go.dev/)
- [Скачать Go](https://go.dev/dl/)
- [Go Playground — онлайн-редактор](https://go.dev/play/)
- [Effective Go — лучшие практики](https://go.dev/doc/effective_go)
- [Go by Example](https://gobyexample.com/)

---

## 🏋️ Практические задания

### Задание 1: Приветствие Go

Напишите программу, которая выводит приветствие на языке Go.

**Ожидаемый результат:**
```
Hello, Go!
```

**Критерии приёмки:**
- Программа компилируется без ошибок
- Используется пакет `fmt` для вывода
- Вывод точно соответствует образцу (с учётом регистра и пунктуации)

**Подсказки:**
- Функция `fmt.Println()` добавляет перенос строки автоматически
- Строки в Go заключаются в двойные кавычки `"..."`

**Начальный код:**
```go
package main

import "fmt"

func main() {
    // Выведите приветствие здесь
}
```

**Ожидаемый вывод:**
```
Hello, Go!
```

**Баллы:** 5

### Задание 2: Информация о себе

Создайте программу, которая выводит две строки: ваше имя и что вы изучаете.

**Ожидаемый результат:**
```
Меня зовут Student
Я изучаю язык Go
```

**Критерии приёмки:**
- Программа выводит ровно две строки
- Каждая строка выводится отдельным вызовом `Println`
- Текст соответствует образцу

**Подсказки:**
- Каждый вызов `fmt.Println()` создаёт новую строку
- Можно использовать несколько вызовов подряд

**Начальный код:**
```go
package main

import "fmt"

func main() {
    // Выведите две строки информации
}
```

**Ожидаемый вывод:**
```
Меня зовут Student
Я изучаю язык Go
```

**Баллы:** 5

### Задание 3: Факты о Go

Выведите три ключевых факта о языке Go в виде нумерованного списка.

**Ожидаемый результат:**
```
1. Go создан в Google
2. Go - компилируемый язык
3. Go поддерживает горутины
```

**Критерии приёмки:**
- Выводится ровно три строки
- Каждая строка начинается с номера и точки
- Используется корректный синтаксис Go

**Подсказки:**
- Номера — это просто часть строки, не отдельные переменные
- Можно использовать три отдельных `Println`

**Начальный код:**
```go
package main

import "fmt"

func main() {
    // Выведите три факта о Go
}
```

**Ожидаемый вывод:**
```
1. Go создан в Google
2. Go - компилируемый язык
3. Go поддерживает горутины
```

**Баллы:** 10

### Задание 4: Многострочный вывод

Создайте программу, которая выводит визитную карточку программиста в рамке из символов.

**Ожидаемый результат:**
```
+------------------+
|  Go Developer    |
|  Version: 1.22   |
+------------------+
```

**Критерии приёмки:**
- Рамка состоит из символов `+`, `-` и `|`
- Текст внутри рамки выровнен
- Все 4 строки выводятся корректно
- Ширина рамки одинакова сверху и снизу

**Подсказки:**
- Символы `+` на углах, `-` для горизонтальных линий, `|` для вертикальных
- Пробелы внутри рамки важны для выравнивания

**Начальный код:**
```go
package main

import "fmt"

func main() {
    // Создайте визитную карточку в рамке
}
```

**Ожидаемый вывод:**
```
+------------------+
|  Go Developer    |
|  Version: 1.22   |
+------------------+
```

**Баллы:** 15

### Задание 5: Структура кода

Напишите программу с комментариями, объясняющими каждую часть кода. Программа должна вывести информацию о структуре Go-программы.

**Ожидаемый результат:**
```
Структура Go программы:
- package: определяет пакет
- import: подключает библиотеки
- func main: точка входа
```

**Критерии приёмки:**
- Программа содержит однострочный комментарий над `package`
- Программа содержит комментарий над `import`
- Программа содержит комментарий над `func main`
- Вывод соответствует образцу

**Подсказки:**
- Однострочные комментарии начинаются с `//`
- Комментарии не влияют на выполнение программы

**Начальный код:**
```go
package main

import "fmt"

func main() {
    // Добавьте комментарии и вывод
}
```

**Ожидаемый вывод:**
```
Структура Go программы:
- package: определяет пакет
- import: подключает библиотеки
- func main: точка входа
```

**Баллы:** 15
